server:
  port: "8080"

routes:
  - path: /users
    service: users
  - path: /orders
    service: orders

services:
  users:
    instances:
      - http://localhost:8081
      - http://localhost:8082
  orders:
    instances:
      - http://localhost:8083
      - http://localhost:8084